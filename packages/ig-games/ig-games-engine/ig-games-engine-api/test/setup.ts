
import { initBeLibMocks } from '@ig/be-utils/test/mocks/BeLibMocks';

beforeAll(async () => {
});

beforeEach(async () => {
});

afterEach(async () => {
});

afterAll(async () => {
});

initBeLibMocks();
