
import { initBeLibMocks } from '@ig/be-lib/test/mocks/BeLibMocks';

beforeAll(async () => {
});

beforeEach(async () => {
});

afterEach(async () => {
});

afterAll(async () => {
});

initBeLibMocks();
